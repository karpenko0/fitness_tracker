# Supabase connection pooler URL for the application (port 6543).
# Add ?pgbouncer=true&connection_limit=1 for Prisma transaction pooling.
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
# Direct Supabase database URL for Prisma migrations (port 5432).
DIRECT_URL=postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres
TELEGRAM_BOT_TOKEN=your_bot_token
JWT_ACCESS_SECRET=your_jwt_access_secret
JWT_ACCESS_TTL_SECONDS=900
REFRESH_TOKEN_TTL_DAYS=30
TELEGRAM_INIT_DATA_MAX_AGE_SECONDS=86400
AUTH_RATE_LIMIT_TELEGRAM_PER_IP=10
AUTH_RATE_LIMIT_TELEGRAM_PER_USER=5
AUTH_RATE_LIMIT_REFRESH_PER_IP=20
AUTH_RATE_LIMIT_REFRESH_PER_SESSION=20
AUTH_RATE_LIMIT_PROFILE_PER_USER=30
ONBOARDING_FALLBACK_ENABLED=true
REDIS_URL=redis://localhost:6379
REDIS_REQUIRED=false
# For Docker Compose use redis://localhost:6379; inside the network use redis://redis:6379.
# SPEC-010 Telegram Stars. Stop-line: keep false until the project owner approves real invoices.
TELEGRAM_PAYMENTS_ENABLED=false
# >= 32 random chars; used in webhook path /api/v1/webhooks/telegram/<secret>
TELEGRAM_WEBHOOK_SECRET=change_me_to_a_long_random_value_at_least_32_chars
# Optional: value passed to setWebhook secret_token, checked via X-Telegram-Bot-Api-Secret-Token
TELEGRAM_WEBHOOK_HEADER_SECRET=
SUBSCRIPTION_JOBS_ENABLED=true
